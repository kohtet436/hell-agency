import 'package:flutter/material.dart';

class WorkSpacePage extends StatelessWidget {
  const WorkSpacePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("work space page"),
      ),
    );
  }
}
